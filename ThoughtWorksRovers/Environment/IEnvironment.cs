namespace ThoughtWorksRovers.Program.Environment
{
    /// <summary>
    /// IEnvironment provides an interface defining the attributes of all environments.
    /// Environments are designed to contain grids and rover controllers, and facilitate 
    /// interaction between the two while keeping both mutually exclusive. The implementation 
    /// of an IEnvironment is wide open and thus no attributes are defined by this interface, 
    /// though the interface remains defined for the purpose of programming to it and future 
    /// modifications.
    /// </summary>
    public interface IEnvironment
    {
       
    }
}
